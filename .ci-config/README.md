# CI Config

Tracked CI configuration files used by active workflows.

Rules:
- Keep only active, reviewed configuration inputs.
- Put runtime outputs in ignored paths (`.ci-out/`, `.ci-cache/`, `.ci-artifacts/`).
