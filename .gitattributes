# Normalize end-of-line to LF in repo; checkout adapts to OS
* text=auto eol=lf

# Treat these as binary to avoid EOL munging
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.npy binary
*.npz binary
*.fits binary
*.csv text eol=lf
*.tsv text eol=lf
