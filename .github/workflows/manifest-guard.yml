name: manifest-guard
on:
  workflow_dispatch: {}
  push:
    branches: [release/zz-tools-0.3.1]
  pull_request:
    branches: [release/zz-tools-0.3.1]

permissions:
  contents: read
  actions: write   # nécessaire pour upload-artifact@v4

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "smoke-ok $GITHUB_RUN_ID"
