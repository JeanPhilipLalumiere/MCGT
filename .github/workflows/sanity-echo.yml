name: sanity-echo

on:
  workflow_dispatch: {}

concurrency:
  group: sanity-echo-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "sanity-echo OK @ $(date -u +%FT%TZ)"
