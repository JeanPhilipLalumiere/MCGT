# MCGT - .gitignore
# NOTE: Le guard CI "tracked files ignored" échoue si un fichier versionné
# matche une règle ci-dessous. Donc: règles FOCALISÉES sur les artefacts générés.

# -------------------------------------------------------------------
# Python / environnements / caches
# -------------------------------------------------------------------
.venv/
.venv*/
venv/
venv*/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# -------------------------------------------------------------------
# Build / packaging (générés)
# -------------------------------------------------------------------
build/
dist/
*.egg-info/
*.dist-info/

# -------------------------------------------------------------------
# Artefacts générés par le projet (toujours locaux)
# -------------------------------------------------------------------
_tmp/
_tmp-figs/
_logs/
_snapshots/
_archives_preclean/
_attic_untracked/
_autofix_sandbox/

# Sorties CI / diag (fichiers et dossiers)
.ci-out/
.ci-logs/
.ci-archive/
.ci-out*
.ci-logs*
.ci-archive*

# Sorties de runs (gros artefacts)
zz-out/
zz-logs/

# Backups locaux
backups/

# -------------------------------------------------------------------
# Fichiers temporaires / éditeurs / OS
# -------------------------------------------------------------------
*.log
*.stderr
*.stdout
*.err
*.out
*.tmp
*.old
*.orig
*.rej
*.bak
*.bak_*
*.save
*.swp
*.swo
*~
.DS_Store

# -------------------------------------------------------------------
# GitHub workflows: uniquement les fichiers temporaires/backup
# (éviter les patterns du genre "*tmp*" qui peuvent attraper un vrai .yml)
# -------------------------------------------------------------------
.github/workflows/*.tmp
.github/workflows/*.bak*
.github/workflows/*.before_*

# -------------------------------------------------------------------
# Sidecars / verrous générés (non versionnés)
# -------------------------------------------------------------------
*.sha256sum
*.lock.json

# -------------------------------------------------------------------
# Legacy tex (si tu veux le garder local uniquement)
# -------------------------------------------------------------------
legacy-tex/

# -------------------------------------------------------------------
# Outils / helpers locaux (non versionnés)
# -------------------------------------------------------------------
zz-tools/
create_and_run_smoke_workflow.sh
garde_fou_universal_v5.sh
restore_add_safe.sh
run_housekeeping_capture.sh
script_patch_publish_safe.sh
step-01-candidates.txt

# Helpers packaging locaux
resolve_twine_license_error.sh
twine_fix_v2.sh
finalize_packaging.sh
finalize_packaging_v2.sh
dist_doctor.sh
sanitize_metadata.sh
repair_pkg_metadata.sh
spdx_futureproof_patch.sh
add_testpypi_readme.sh
add_pypi_stable_install_and_badge.sh
bump_*_and_publish.sh
publish_pypi_release.sh
pypi_upload_*.sh

# Variantes/backups de configs
.pre-commit-config.yaml.*
environment.disabled
