# Assets

Canonical publication assets live here:
- `zz-data/` validated datasets
- `zz-figures/` publication figures
- `zz-manifests/` release manifests and integrity metadata
- `zz-schemas/` validation schemas

Local runtime caches and transient artifacts must stay ignored and out of version control.
