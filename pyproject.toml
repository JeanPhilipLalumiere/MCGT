[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "zz-tools"
version = "0.0.0"
description = "MCGT utilities"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["zz_tools*"]
