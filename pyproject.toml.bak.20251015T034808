[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

# Facultatif: si tu utilises discovery auto:
[tool.setuptools.packages.find]
where = ["."]
include = ["zz_tools*"]
