# src (normalized)

Code source Python actif du package `mcgt` pour la release v4.0.0.

## Contenu

- `src/mcgt/` : modules utilises par les scripts et tests actifs.
- Les caches Python ne sont pas conserves dans le depot.
