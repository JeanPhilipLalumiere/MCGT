# tests (normalized)

Suite de tests active pour la release v4.0.0.

## Notes

- Les fichiers `test_*.py` sont la source de verite.
- Les caches Python (`__pycache__`) sont supprimes pendant le menage.
- La CI execute un sous-ensemble cible, et des tests complementaires peuvent etre lances localement.
