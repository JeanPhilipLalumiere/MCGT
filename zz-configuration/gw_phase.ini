# gw_phase.ini — Configuration globale des pipelines MCGT

[scan]
# Plage de fréquences pour la grille log-uniforme
# Fréquence minimale (Hz)
fmin = 1e-4
# Fréquence maximale (Hz)
fmax = 1e2
# Pas en log₁₀(f) : N = floor((log10(fmax) - log10(fmin)) / dlog) + 1
dlog = 0.01
