# Zenodo Sync Status

- Version in `.zenodo.json` set to 2.4.0.
- Tag `v2.4.0` exists on GitHub and is ready for a GitHub Release.
- Next step: publish the GitHub Release from tag `v2.4.0` to trigger Zenodo DOI refresh.
